# ML-level-1
We will build neural networks from scratch using basic maths and algorithms

Basic representation and function of neural networks:

nodes and verticies representation of a neural network
back propagation using automatic differentiation
customizable functions/states for each node (allows extension)




